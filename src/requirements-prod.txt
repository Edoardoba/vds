# Production requirements for Render deployment

# Web framework and server
fastapi==0.115.5
uvicorn[standard]==0.34.0  # SYNCED: Match dev version for consistency
gunicorn==23.0.0
python-multipart==0.0.20  # SYNCED: Match dev version

# AWS SDK
boto3==1.35.77  # SYNCED: Latest stable version
botocore==1.35.77  # SYNCED: Must match boto3 version

# Configuration & environment
pydantic==2.11.1  # SYNCED: Match dev version for type consistency
pydantic-settings==2.11.0  # SYNCED: Match dev version
python-decouple==3.8
python-dotenv==1.0.1
PyYAML==6.0.2
structlog==23.2.0

# Data stack (Python 3.13 compatible)
numpy==1.26.4  # SYNCED: Match dev version for consistency
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.6.1
statsmodels==0.14.4
openpyxl==3.1.5
xlrd==2.0.1

# Visualization
matplotlib==3.9.2
seaborn==0.13.2

# Networking
httpx==0.28.1  # SYNCED: Match dev version

# Database (for persistence and caching)
SQLAlchemy==2.0.36
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL adapter (optional)

# LangGraph for multi-agent workflow orchestration
langgraph==0.2.34
langchain==0.3.15  # ADDED: Full langchain package
langchain-core==0.3.79  # SYNCED: Match dev version
langchain-anthropic==0.3.3  # SYNCED: Match dev version
langchain-text-splitters==0.3.11  # ADDED: Missing in prod
